# Linux Golden Image

This folder contains scripts used to prepare the Ubuntu VM before the local AI agent stack is installed.

The goal is to produce a hardened, reusable base image that already has the basic OS preparation completed.

## Files

| File | Purpose |
|------|---------|
| `linux-initial-config.sh` | Applies the initial Linux configuration and package setup used while building the VM golden image. |
