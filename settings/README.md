# Settings

This folder contains configuration data used by the installed system.

Its purpose is to separate editable settings from the Python code and setup scripts.

## Files and Subfolders

| Item | Purpose |
|------|---------|
| `repos/` | Repository configuration used to decide which Git repositories the agent can work on. |

Read more in folder `settings/repos`.
