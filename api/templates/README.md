# API Templates

This folder is currently a compatibility placeholder.

Rendered web pages are now stored in the repo-root `www/` folder instead of here.

This folder may be removed later or reused if the application introduces API-local template assets again.
